<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
    
<mapper namespace="com.lgy.YoRiZoRi.detail_review.dao.ReviewDAO">
	<select id="list" resultType="com.lgy.YoRiZoRi.detail_review.dto.ReviewDTO">
		select  review_id, recipe_id, member_id, content, rating, image, created_at
		from review
		where review_id=#{review_id}
	</select>

	<insert id="write">
		insert into review
		(review_id, recipe_id, member_id, content, rating, image, created_at) values
		(#{review_id}, #{recipe_id}, #{member_id}, #{content},
		 #{rating}, #{image}, #{created_at})
	</insert>

</mapper>