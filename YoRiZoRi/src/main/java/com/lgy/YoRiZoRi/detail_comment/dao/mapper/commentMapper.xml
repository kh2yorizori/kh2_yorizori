<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
    
<mapper namespace="com.lgy.YoRiZoRi.detail_comment.dao.CommentDAO">
	<select id="list" resultType="com.lgy.YoRiZoRi.detail_comment.dto.CommentDTO">
		select nickname from member_tb where member_id=#{member_id}
		select cmt_content, created_at from comment_tb where comment_id=#{comment_id}
	</select>

	<insert id="write">
		insert into comment_tb
		(comment_id, ref_id, cmt_step, cmt_depth,
		 recipe_id, member_id, cmt_content, created_at) values
		(#{comment_id}, #{ref_id}, #{cmt_step}, #{cmt_depth},
		 #{recipe_id}, #{member_id}, #{cmt_content}, #{created_at})
		 
	</insert>

</mapper>